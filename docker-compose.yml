version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_DB: apiSistemaNotas
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: upacifico